spring:
  datasource:
    url: jdbc:mysql://10.116.64.14:13306/project_be10_team3_dev?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true          # ←★ 콘솔에 SQL 출력
    properties:             # ❶ 포매팅 옵션 (있으면 유지)
      hibernate:
        format_sql: true

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

########################
# ✨  Actuator 노출  ✨
########################
management:
  endpoints:
    web:
      exposure:
        include: health, info, mappings
  endpoint:
    health:
      show-details: always

########################
# 🚀  Hibernate 로그 레벨
########################
logging:
  level:
    org.hibernate.SQL: DEBUG     # ←★ 실제 SQL
    org.hibernate.type: TRACE    #   바인딩 파라미터까지 확인하고 싶으면 추가