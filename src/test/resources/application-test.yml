spring:
  config:
    activate:
      on-profile: test

    cloud:
      config:
        enabled: false

  # H2 인메모리 DB 설정
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false;MODE=MySQL
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop  # 테스트마다 스키마 생성/제거
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        auto_quote_keyword: true
        highlight_sql: true
    database-platform: org.hibernate.dialect.H2Dialect

  h2:
    console:
      enabled: true
      path: /h2-console

logging:
  level:
    com.nhnacademy.bookstoreorderapi.order: debug
    org.springframework: info
    org.hibernate: info